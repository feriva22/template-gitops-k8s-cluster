apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: argocd
  namespace: argocd
spec:
  values:
    redis-ha:
      enabled: false
    controller:
      replicas: 1
    server:
      replicas: 1
      ingress:
        enabled: false